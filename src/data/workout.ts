import type { Workout } from '@/types/workout';

/*
 * Workout data now comes from the FitLog API.
 *
 * This file intentionally does not contain local image paths because
 * the official assignment requires workout images to come from the API.
 */
export const workouts: Workout[] = [];
